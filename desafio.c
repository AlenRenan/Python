#include <stdio.h>
#include <stdlib.h>

int main(){
    //Repetição com o while
    cont = 0;
    while (cont <10) {
        printf ("\n Aluno %d", (cont+1))
        cont++
    }
}
    //repetição com o for
    for (cont=0; cont<10:cont++){
        printf ("\n Aluno %d", (cont+1));
    }
    //Repetição com do-while
    cont = 0;
    do {
        printf ("\n Aluno %d", (cont+1));
        while(cont<10);
    }

    //Começo do desafio











